// const validUserNames = (usernames) => {
//     usernames.filter((name) => name.length < 10)
// }

// function, not a function expression
function validUserNames(usernames) {
    return usernames.filter((name) => name.length < 10)
}
